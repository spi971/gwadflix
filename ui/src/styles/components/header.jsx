import styled from "styled-components";
// Create new container with styled component
export const HeaderContainer = styled.div`
  padding: 0 4 rem;
  .logo {
    img {
      height: 5rem;
    }
  }
  button {
    padding: 0.5rem 1rem;
    background-color: #e50914;
    border: none;
    cursor: pointer;
    border-radius: 0.2rem;
    color: white;
    font-size: 1.05rem;
    font-weight: bolder;
  }
`;
